// Presage SDK — real-time focus/stress/engagement signal from camera.
// Feeds both the Gemini reasoning core and the feedback model directly.

export async function getRealtimeSignal({ sessionId }) {
  // TODO: connect to Presage SDK stream, return current focus/stress/engagement reading
  throw new Error("presage.getRealtimeSignal not implemented");
}
